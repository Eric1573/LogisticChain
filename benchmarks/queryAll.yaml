test:
  workers:
    number: 5   
  rounds:
    - label: queryAll
      description: queryAll Benchmark
      txNumber: 500
      rateControl:
        type: fixed-rate
        opts:
          tps: 25
      workload:
        module: workload/queryAll.js

     
    
